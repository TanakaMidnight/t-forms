import React from 'react';
import { render } from '@testing-library/react';
import AppHeader from '../AppHeader';

describe('AppHeader', () => {
  it('レンダリングできる', () => {
    render(<AppHeader />);
    // TODO: 適切なアサーションを追加
  });
});
